<!DOCTYPE html>
<html>

<body>
    <header>
        <%- include('../partials/header'); %>
    </header>

    <head>
        <link rel="stylesheet" type="text/css" href="/css/report.css">
        <script src="/js/report.js"></script>
    </head>

    <div style="float: right; margin-top: 0.8em; margin-right: 0.8em;">
        <button type="button" id="exelExport" class="btn btn-sm excelButton">Excel Export</button>
    </div>

    <%- include('../partials/jobTabFilter') %>

    <div class="container response">
        <%
            for (var i =0; i < 2;  i++ ) { %>
        <%- include('../partials/reportSegment') %>
        <% }
            %>
        <!-- Here is an example of how we cam pass an array of time card objects to the report from express: include('../partials/reportSegment', {data: someObject}) -->
    </div>
    <div class="container" style="margin-top: 3em; margin-bottom: 5em">
        <div class="row">
            <div class="col-3 table-responsive">
                <%- include('../partials/reportSummary') %>
            </div>
            <div class="col-3" style="display: flex; align-items: flex-end;">
                <button type="button" id="exelExport" class="btn btn-md excelButton">Approve
                    Period</button>
            </div>
        </div>
    </div>
</body>

</html>